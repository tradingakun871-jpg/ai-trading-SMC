const express=require('express');const path=require('path');const https=require('https');const{analyze}=require('./src/smc-engine');const tracker=require('./src/trade-tracker');const app=express();const PORT=process.env.PORT||3000;app.use(express.json({limit:'2mb'}));app.use(express.static(path.join(__dirname,'public')));const mt5State={connected:false,lastSeen:null,account:null,symbols:{}},signalState={},outcomeState={};
function normalizeSymbol(s=''){s=String(s).toUpperCase();if(s.includes('XAUUSD'))return'XAUUSD';if(s.includes('BTCUSD'))return'BTCUSD';return s.replace(/[^A-Z0-9]/g,'').slice(0,24)}function bridgeAuth(q,r,n){const e=process.env.MT5_BRIDGE_TOKEN;if(!e||q.get('x-bridge-token')===e)return n();r.status(401).json({ok:false,error:'unauthorized'})}function telegram(text){const token=process.env.TELEGRAM_BOT_TOKEN,chat=process.env.TELEGRAM_CHAT_ID;if(!token||!chat)return Promise.resolve(false);return new Promise(resolve=>{const body=JSON.stringify({chat_id:chat,text}),q=https.request({hostname:'api.telegram.org',path:`/bot${token}/sendMessage`,method:'POST',headers:{'Content-Type':'application/json','Content-Length':Buffer.byteLength(body)}},r=>{r.resume();resolve(r.statusCode>=200&&r.statusCode<300)});q.on('error',()=>resolve(false));q.write(body);q.end()})}
function trackOutcomes(symbol,candles){const before=new Map(tracker.history(symbol,200).map(t=>[t.id,t.result])),list=tracker.update(symbol,candles);for(const t of list)if(t.status==='CLOSED'&&!before.get(t.id)&&outcomeState[t.id]!==t.result){outcomeState[t.id]=t.result;const p=tracker.performance(symbol,'all');telegram(`TRADE RESULT ${symbol}\n#${t.id} ${t.side} ${t.result}\nEntry: ${t.entry}\nExit: ${t.exitPrice}\nPnL: ${t.pnlPips} pips\nWin Rate: ${p.winRate}%\nNet: ${p.netPips} pips`)}}
function runSmc(symbol){const s=mt5State.symbols[symbol];if(!s?.M15?.candles||!s?.M5?.candles||!s?.M3?.candles)return null;trackOutcomes(symbol,s.M3.candles);const pip=symbol==='XAUUSD'?Number(process.env.PIP_SIZE_XAUUSD||.1):Number(process.env.PIP_SIZE_BTCUSD||1),result=analyze({symbol,m15:s.M15.candles,m5:s.M5.candles,m3:s.M3.candles,pipSize:pip});s.smc=result;const sig=result.signal;if(sig){const key=[sig.side,sig.entry,sig.sl,sig.tp2].join(':');if(signalState[symbol]!==key){signalState[symbol]=key;const t=tracker.addSignal(symbol,sig,{bias:result.bias});telegram(`SMC SIGNAL ${symbol}\n${sig.side} LIMIT${t?` #${t.id}`:''}\nEntry: ${sig.entry}\nSL: ${sig.sl} (50 pips)\nTP1: ${sig.tp1} (50 pips)\nTP2: ${sig.tp2} (100 pips)\nBE: +30 pips\nZone: ${sig.zone}\nM15 Bias: ${result.bias}\nAuto Trade: OFF`)}}return result}
app.get('/health',(q,r)=>r.json({status:'ok',service:'ai-trading-smc',engine:'SMC V2 + Trade Tracker V3.1',database:tracker.performance().database,databaseReady:tracker.performance().ready}));app.get('/api/status',(q,r)=>r.json({status:'online',engine:'SMC V2',tracker:'V3.1',database:tracker.performance().database,databaseReady:tracker.performance().ready,symbols:['XAUUSD','BTCUSD'],mt5:{connected:mt5State.connected,lastSeen:mt5State.lastSeen},telegram:{configured:Boolean(process.env.TELEGRAM_BOT_TOKEN&&process.env.TELEGRAM_CHAT_ID)},execution:{enabled:false,type:'limit',slPips:50,tp1Pips:50,tp2Pips:100,breakEvenAfterPips:30}}));function save(body,history){const symbol=normalizeSymbol(body.symbol),tf=String(body.timeframe||'').toUpperCase();if(!symbol||!['M3','M5','M15'].includes(tf))return null;const now=new Date().toISOString();mt5State.connected=true;mt5State.lastSeen=now;mt5State.account=body.account||mt5State.account;mt5State.symbols[symbol]??={};let d;if(history){const candles=(Array.isArray(body.candles)?body.candles:[]).slice(-300).map(c=>({time:c.time,open:+c.open,high:+c.high,low:+c.low,close:+c.close,tickVolume:+(c.tickVolume||0)})).filter(c=>[c.open,c.high,c.low,c.close].every(Number.isFinite));if(!candles.length)return null;d={...candles.at(-1),bid:+(body.bid||0),ask:+(body.ask||0),receivedAt:now,historyBars:candles.length,candles}}else d={time:body.time||null,open:+body.open,high:+body.high,low:+body.low,close:+body.close,tickVolume:+(body.tickVolume||0),bid:+(body.bid||0),ask:+(body.ask||0),receivedAt:now};mt5State.symbols[symbol][tf]=d;return{symbol,tf,d}}
app.post('/api/mt5/ohlc',bridgeAuth,(q,r)=>{const x=save(q.body||{},false);r.status(x?200:400).json({ok:!!x,executionEnabled:false})});app.post('/api/mt5/history',bridgeAuth,(q,r)=>{const x=save(q.body||{},true);if(!x)return r.status(400).json({ok:false});const smc=runSmc(x.symbol);r.json({ok:true,symbol:x.symbol,timeframe:x.tf,historyBars:x.d.historyBars,smcStage:smc?.stage||'COLLECTING_HISTORY',performance:tracker.performance(x.symbol,'all'),executionEnabled:false})});app.get('/api/smc/:symbol',(q,r)=>{const result=runSmc(normalizeSymbol(q.params.symbol));result?r.json({ok:true,...result}):r.status(503).json({ok:false,stage:'COLLECTING_HISTORY'})});app.get('/api/performance/:symbol',(q,r)=>r.json({ok:true,...tracker.performance(normalizeSymbol(q.params.symbol),String(q.query.period||'all').toLowerCase())}));app.get('/api/performance-summary',(q,r)=>r.json({ok:true,...tracker.summary()}));app.get('/api/history/:symbol',(q,r)=>r.json({ok:true,trades:tracker.history(normalizeSymbol(q.params.symbol),q.query.limit)}));app.get('/api/mt5/status',(q,r)=>{const age=mt5State.lastSeen?Date.now()-new Date(mt5State.lastSeen):null;r.json({connected:age!==null&&age<30000,lastSeen:mt5State.lastSeen,ageSeconds:age===null?null:Math.round(age/1000),account:mt5State.account,symbols:mt5State.symbols})});app.get('/api/orders/pending',bridgeAuth,(q,r)=>r.json({executionEnabled:false,orders:[]}));app.post('/api/orders/report',bridgeAuth,(q,r)=>r.json({ok:true,executionEnabled:false}));app.use((q,r)=>r.sendFile(path.join(__dirname,'public','index.html')));tracker.init().then(()=>app.listen(PORT,'0.0.0.0',()=>console.log(`AI Trading SMC + Trade Tracker V3.1 PostgreSQL running on port ${PORT}`))).catch(e=>{console.error('PostgreSQL initialization failed',e);process.exit(1)});